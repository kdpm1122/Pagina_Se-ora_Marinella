{
  "name": "mueblescatalog",
  "version": "1.0.0",
  "description": "Catalogo de muebles con carrito y pedido por WhatsApp",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon server.js",
    "build": "esbuild public/js/catalogo.js --minify --outfile=public/dist/catalogo.min.js && esbuild public/js/carrito.js --minify --outfile=public/dist/carrito.min.js && esbuild public/js/color-fondo.js --minify --outfile=public/dist/color-fondo.min.js && esbuild public/css/styles.css --minify --outfile=public/dist/styles.min.css && esbuild public/css/inicio.css --minify --outfile=public/dist/inicio.min.css",
    "vercel-build": "npm run build",
    "start": "node server.js"
  },
  "keywords": [
    "muebles",
    "catalogo",
    "ecommerce"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cloudinary": "^2.10.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.2",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.2.0",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "nodemon": "^3.1.14"
  }
}
